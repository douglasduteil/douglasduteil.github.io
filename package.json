{
  "name": "douglasduteil.github.io",
  "private": true,
  "packageManager": "yarn@3.3.0",
  "scripts": {
    "build": "yarn workspace www build",
    "dev": "yarn workspace www dev",
    "format": "yarn workspace www format",
    "lint": "yarn workspace www lint",
    "preview": "yarn workspace www preview",
    "test": "yarn workspace www test:unit"
  },
  "workspaces": [
    "www"
  ]
}
